import { requireUserManagementAdmin } from "@/lib/permissions/user-management";

/** Admin-only access for device authorization management. */
export { requireUserManagementAdmin as requireDeviceAdmin };
