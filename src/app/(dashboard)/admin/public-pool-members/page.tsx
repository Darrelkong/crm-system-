export const dynamic = "force-dynamic";

import { PublicPoolMembersClient } from "./public-pool-members-client";

export default function AdminPublicPoolMembersPage() {
  return <PublicPoolMembersClient />;
}
